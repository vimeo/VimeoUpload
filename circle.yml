machine:
  xcode:
    version: 8.0

dependencies:
    override:
        - bundle install
    cache_directories:
        - "Pods"
        - "vendor/bundle"

test:
  override:
    - FASTLANE_SKIP_UPDATE_CHECK=1 bundle exec fastlane test
